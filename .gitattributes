# 统一行尾：仓库内一律 LF，避免 Windows 上的 core.autocrlf 把每个文件都
# 标记成"已改动"，让真实改动淹没在行尾噪音里（本项目实际踩过）。
#
# 背景：本机 core.autocrlf=true，而工作区文件是 LF。结果是 git 在 checkout
# 时把文件转成 CRLF、提交时又转回去，diff 里混入整文件的伪改动。维护模式下
# 这一条足以让 code review 失效，所以显式关掉。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary

# Windows 批处理脚本必须保留 CRLF，否则 cmd 可能解析异常。
# 当前仓库未跟踪任何 .bat/.cmd，此项为未来加入时的保护。
*.bat text eol=crlf
*.cmd text eol=crlf
